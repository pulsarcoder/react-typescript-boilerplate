import * as React from 'react';

export default (props) => {
    return (
        <header className="header">
            <a className="logo" href="/">Brand</a>
            <nav>
                <ul>
                    <li>
                        <a href="/">Menu1</a>
                    </li>
                </ul>
            </nav>
        </header>
    );
}