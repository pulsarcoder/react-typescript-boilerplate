import * as React from 'react';

export default (props) => {
    return (
        <aside className="sidebar">
            <div>
                <h2>Hey im sidebar</h2>
            </div>
        </aside>
    );
}